server:
  port: 8848
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
  instance:
    prefer-ip-address: true
spring:
  profiles:
      active: native
  application:
    name: aigou-config-server
  cloud:
    config:
      server:
        native:
          search-locations: E:/aigou-config
      #  git:
      #    uri: https://github.com/wilsonGZ/aigou-config.git
      #    username: wilsonGZ
       #   password: gz910121